# Release assets

This orphan branch/tag exists only to anchor GitHub Release asset uploads
for the `pangaln` project's `cluster` branch -- it has no source relationship
to spaln's actual development history (`master`/`pangap`), intentionally, so
tagging a release here never drags in unreviewed commits from an active
branch.
